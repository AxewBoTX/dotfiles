# Vim Config

This is just a basic vim config which I barely even use, it don't even have any plugins installed. It just contains the basic keybinds and settings which can be used to edit files without much problem if needed.
